#include "../../s21_string.h"

int s21_sscanf(const char *str, const char *format, ...) {
  long int kostylishe = str - format;
  if (kostylishe) {
    ;
  }
  // if (str && format) kostylishe = 0;
  return -1;
}